The task is an item and the home. Read `<home>/spec.md` and the axis file the
item names, `<home>/code-axis.md` unless it says otherwise, in full and
verbatim. The item names your step and your files; the axis names what an agent
of your name does and this box's tools. You are cold; the two files and the
item are the world.
